# Examples

This folder contains practical examples for different product types.

Each example shows how the skill turns a vague product idea into an architecture decision before implementation.

Use these examples as reference patterns when running the workflow in `SKILL.md`.
